---
name: wechat-formatter
description: "微信公众号排版工具。将普通文章/Markdown内容转换为风格统一的微信公众号HTML排版，包括：标题样式（左边框装饰、颜色分级）、正文样式、引用块、代码块、步骤列表、卡片式分节导航、强调文字、分割线等。参考风格来自「摸鱼小李」公众号。"
description_zh: "将文章内容转换为微信公众号图文排版格式"
description_en: "Convert article content to WeChat Official Account HTML layout"
---

# 微信公众号排版技能

将任意文章、Markdown、或纯文字内容，自动排版为**微信公众号可直接粘贴使用**的 HTML 格式，风格参考「摸鱼小李」公众号。

## 工作流程

1. **接收内容** → 用户提供原始文章、Markdown 或纯文字
2. **分析结构** → 识别标题层级、段落、代码块、列表、引用等
3. **生成排版** → 输出完整 HTML，调用 `scripts/format.py` 或直接按模板手工生成
4. **预览确认** → 输出 HTML 预览文件，供用户查看效果
5. **输出交付** → 提供可直接复制到微信公众号编辑器的 HTML 内容

## 排版风格规范

排版细节完整定义见 `references/style-guide.md`，核心要点如下：

### 配色方案
- 主色：`#1a1a1a`（正文）
- 强调色：`#07c160`（微信绿，用于左边框装饰、按钮）
- 标题色：`#333`
- 辅助色：`#666`（副标题、说明文字）
- 背景色：`#fff`（主背景），`#f7f7f7`（代码块/引用背景）
- 链接色：`#576b95`

### 标题系统（三级）
- **H1 主标题**：24px，加粗，底部绿色下划线装饰，`margin-bottom: 24px`
- **H2 章节标题**：带绿色左边框（4px solid #07c160），浅绿背景，18px，加粗
- **H3 小节标题**：带圆形数字序号装饰或 emoji 前缀，16px，加粗

### 正文样式
- 字号：16px
- 行高：1.8
- 颜色：`#3d3d3d`
- 段间距：`margin-bottom: 20px`
- 不首行缩进（微信公众号规范）

### 特殊组件
- **引用块**：左侧 4px 绿色竖条，浅灰背景 `#f7f7f7`，内边距 12px 16px
- **代码块**：灰色背景 `#f6f8fa`，圆角 6px，等宽字体，关键字橙红色 `#e96900`
- **步骤列表**（Step 1/2...）：圆形序号 + 加粗标题 + 缩进说明
- **卡片组**（Part 01/02...）：横向排列，带编号圆圈，绿色强调
- **小贴士/提示框**：浅黄/浅绿背景，emoji 前缀，圆角
- **分割线**：细灰线 `1px solid #ebebeb`，上下留白 24px

### 可复制按钮块
重要的可操作命令/内容，用带边框的代码框 + 顶部标注"📋 复制发给助手"展示。

## 使用方式

### 方式1：直接生成（推荐）

用户提供内容后，直接按本技能的模板手工生成 HTML，无需运行脚本。

输出格式：
```
以下是排版后的 HTML，可直接复制到微信公众号编辑器：

[HTML 内容]

预览文件已保存到：[路径]
```

### 方式2：运行脚本

```bash
python scripts/format.py --input article.md --output output.html
```

参数：
- `--input`：输入文件（Markdown 或 txt）
- `--output`：输出 HTML 文件路径（默认 `output.html`）
- `--title`：文章标题（可选，会覆盖正文中的 H1）
- `--theme`：主题色（默认 `#07c160`，可自定义）

## 内容结构识别规则

| 原始内容 | 转换为 |
|---------|--------|
| `# 标题` / 一级标题 | H1 主标题样式 |
| `## 章节` / 二级标题 | H2 绿色左边框章节标题 |
| `### 小节` / 三级标题 | H3 带序号小节标题 |
| `> 引用` | 绿色左条引用块 |
| `` ` `` 代码 `` ` `` | 行内代码（橙红色） |
| ```` ``` ```` 代码块 | 灰色背景代码框 |
| `**加粗**` | 加粗 + 强调色文字 |
| `Step 1:` / 步骤 | 圆形序号步骤组件 |
| `Part 01` / 部分卡片 | 卡片式分节导航组件 |
| `📋 复制...` 后跟代码 | 可复制命令框 |
| `---` 分割线 | 细灰分割线 |
| 普通段落 | 标准正文段落 |

## 注意事项

- 微信公众号编辑器不支持外部字体，不要引用 Google Fonts
- 所有样式必须**内联（inline style）**，不能用 `<style>` 标签（粘贴到微信编辑器时会丢失）
- 图片使用 `max-width: 100%`，确保移动端适配
- 不使用 JavaScript（微信不支持）
- 整体宽度约 94%，两侧留白，适配微信移动端
